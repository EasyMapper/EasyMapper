plugins {
    id 'java-library'
}

repositories {
    jcenter()
}

dependencies {
    testImplementation(project(":japper"))
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.6.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.6.0'
}

test {
    useJUnitPlatform()
}
